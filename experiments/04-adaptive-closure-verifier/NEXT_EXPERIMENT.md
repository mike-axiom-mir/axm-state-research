# Next Experiment — Real-Project Closure Trial

Apply the verifier to the canonical Workfloor Sentinel checks without duplicating their source.

## Single strongest gate

Hold back a deterministic mutation set unknown to dependency learning. Pass only if:

```text
silent stale outputs == 0
final oracle equality == true
all repairs retain provenance
audit + replay work < full oracle work
```

Compare declared-risk auditing, observed file/config reads, and their combination. Plant direct file dependency omissions, derived/report chains, incomplete evidence access, conditional reads, and dormant invalid metadata. Automatically minimize every miss. Do not add an AI model until the deterministic closure boundary is understood.
