# Next Experiment

The next experiment must be chosen after the frozen score. If the candidate fails, attack the minimized miss without changing Experiment 06. If it passes, the strongest next pressure is an additional canonical version boundary where handler source changes, checkpoints cross process/runtime boundaries, and opaque access has no useful public parameter shape.

Any next lane should keep total-work accounting, unlabeled held-out inputs, trusted-checkpoint verification, automatic miss minimization, and explicit human merge authority.

