# Next Experiment — Cross-Version Opaque Recovery Challenge

Freeze training before evaluator source changes, then score a later canonical project version where opaque helpers expose no useful path-shaped public parameters. Include one checkpoint whose canonical source cannot be established locally, requiring a retained unresolved abstain/escalate outcome rather than reconstruction.

Keep the Experiment 06 disciplines:

- no held-out risk labels, oracle answers, or training probes;
- total sparse+audit+replay+reconstruction work in the gate;
- oracle scoring only after policy action;
- automatic retention and minimization of every miss;
- checkpoint provenance and explicit untrusted-replay prohibition; and
- human merge authority.

The primary question is whether a bounded non-oracle policy can remain useful when neither observed reads nor ordinary contract shape exposes the missing dependency. A failed or unresolved result should be published without widening labels or changing held-out inputs.
