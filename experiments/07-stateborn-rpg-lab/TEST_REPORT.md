# AXM Stateborn RPG Lab v0.5.0 — Test Report

Tested: 2026-09-04

## Result

**PASS within the v0.5.0 local experimental boundary**

The actor-capsule engine and retained v0.1–v0.4 experiments passed. This does
not prove human-state transfer, secure networking, machine consciousness,
open-ended emergence, or a generated RPG.

## Automated verification

62 / 62 tests passed:

- 12 original canonical-state and receipt-integrity tests;
- 13 sparse living-world and autonomous-reciprocity tests;
- 11 curiosity-loop tests;
- 13 coexistence tests;
- 13 actor-capsule ownership, consent, return, and recovery tests.

The capsule tests verify:

- only allowlisted, consented public paths enter a capsule;
- private notes and recovery tokens remain outside the projection;
- tampered capsules refuse before composition;
- different owners occupy distinct session namespaces;
- namespace collisions refuse rather than fuse;
- session actions cannot mutate either source;
- matching independent signal receipts can project one shared thread;
- return packets have proposal authority only;
- each source accepts only explicitly selected return paths;
- forged and stale return packets fail closed;
- detachment removes a projection while retaining separation evidence;
- exact replay reconstructs composition, collaboration, return, and detach.

Static validation covers four offline HTML entrypoints, 14 JavaScript files,
all local navigation, the hosting declaration, and the absence of external
runtime dependencies.

## Sixteen-run capsule probe

- valid Aster and Briar capsules: 16 / 16;
- distinct owner namespaces: 16 / 16;
- one receipt-backed shared signal: 16 / 16;
- sources unchanged before acceptance: 16 / 16;
- Aster signal accepted, Briar no fields accepted: 16 / 16;
- forged return digest refused: 16 / 16;
- exact replay: 16 / 16.

These are deterministic in-process runs over fictional data. The allowlists,
source actors, crossing plan, shared-signal rule, and return policies are
authored experimental fixtures.

## Retained experiment evidence

- v0.1 preserves the deterministic receipt chain and failed scale profile;
- v0.2 preserves sparse million-cell state and share/wait counterfactuals;
- v0.3 preserves the outcome-blind curiosity probe;
- v0.4 preserves the three-seat coexistence/referee probe.

## Browser interaction verification

**UNKNOWN in this session.** The cloud browser refused both localhost and
`file:` delivery under its URL policy, so no rendered-surface claim is made.
Automated engine, bundle syntax, navigation, and static checks above are
complete. A future browser seat should run the shortest visible route:
compose both capsules → emit matching signals → inspect the shared thread →
build asymmetric returns → forge one packet → detach → verify replay.

## Truth boundary

Observed: bounded structured projections from two independent fictional actor
sources can coexist, create shared receipt evidence, return selected paths,
separate, and replay without giving the session source-write authority.

Not proven:

- movement or representation of a whole human identity;
- cross-device transport, accounts, authentication, encryption, or hostile
  multiplayer safety;
- compatibility between arbitrary game schemas;
- subjective curiosity, machine desire, or mutual understanding;
- fun, narrative value, autonomous emergence, or production readiness;
- GitHub CI, merge, release, or canon state.
